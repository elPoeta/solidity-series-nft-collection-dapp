# NFT Collection Dapp

```sh
 yarn add -D @nomicfoundation/hardhat-chai-matchers @nomiclabs/hardhat-ethers @typechain/ethers-v5 @typechain/hardhat @types/chai @types/mocha @types/node chai dotenv ethers hardhat mocha ts-node typechain @openzeppelin/contracts
```
